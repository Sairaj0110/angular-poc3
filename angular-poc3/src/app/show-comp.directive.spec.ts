import { ShowCompDirective } from './show-comp.directive';

describe('ShowCompDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowCompDirective();
    expect(directive).toBeTruthy();
  });
});
